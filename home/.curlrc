# Curl proxy switch up in need

# use @justmao945's `mallory`, both modes supported
#socks5=http://127.0.0.1:1314 # ssh-socks
#proxy=http://127.0.0.1:1315  # http(s)
#proxy-user=<user>:<pass>
noproxy=localhost,127.0.0.1
