iconv -f shift-jis -t utf-8 < $in > $out
iconv -f euc-jp -t utf-8 < $in > $out

#iconv 会直接把转成utf8后的内容打印到终端，这样就可以不用调终端的编码了
#iconv -l 列出所有支持的编码，你可以找一些日文编码，iconv -l | grep JIS 你可以看到实际上 jis 也是有很多变种的嗯

# vim: set ft=sh:
